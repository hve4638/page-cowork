{
  "name": "cowork-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "node --experimental-sqlite --watch src/index.ts",
    "start": "node --experimental-sqlite src/index.ts",
    "check": "tsc --noEmit",
    "seed-admin": "node --experimental-sqlite scripts/seed-admin.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@types/ws": "^8.5.12",
    "typescript": "~5.6.2"
  }
}
