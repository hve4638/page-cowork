# HTTPS 종단 예시 (Caddy). 마이크 녹음은 보안 컨텍스트가 필요하므로 IP·도메인으로 접속하려면 HTTPS 여야 한다.
# 도메인이 있으면 첫 줄을 도메인으로 바꾸면 Let's Encrypt 인증서를 자동으로 받는다.
# 사설망에서 IP 로만 쓰면 `tls internal` 로 자체 서명하고 브라우저 경고를 한 번 통과한다.
https://192.168.0.10 {
    tls internal
    reverse_proxy 127.0.0.1:80    # .env 의 COWORK_PORT
}
